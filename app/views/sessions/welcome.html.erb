<h1>Welcome to Private Event App</h1>

<%# ----- add these lines here: ----- %>

    <% if current_user %>
      <!-- current_user will return true if a user is logged in -->
      <%= "Logged in as #{current_user.username}" %> | <%= link_to 'Log Out', logout_path, method: :delete %>
    <% else %>
      <!-- not logged in -->
    <%= link_to 'Log In', login_path %> or <%= link_to 'Sign Up', new_user_path %>
    <% end %>
    <hr>

    <%# ----- end of added lines ----- %>

<!--
<% if current_user %>
    <h1>You are Logged In, <%= current_user.username %></h1>
    <%= button_to "Logout", '/logout', method: :delete %>
<% end %>
-->
<% if !current_user %>
<div>
<%= button_to "Sign Up", '/users/new', method: :get%><br>
<%= button_to "Login", '/login', method: :get%>
</div>
<% end %>
